// Placeholder — demo UI is being rebuilt as a React app on top of @mindmaplib/react.
// See docs/specs/MML-B-0006_DEMO_SPEC.md for the target architecture.

import './style.css'

const app = document.getElementById('app')!
app.innerHTML = `
  <div class="reconstruction">
    <h1>mindmaplib demo</h1>
    <p>Under reconstruction — moving to React + @mindmaplib/react adapter.</p>
    <p>See <a href="https://github.com/AndrewArto/mindmaplib">github.com/AndrewArto/mindmaplib</a></p>
  </div>
`
